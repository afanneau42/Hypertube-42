import React from 'react';
import axios from 'axios';
import config from '../config/config';

const NotFound = () => (
    <div>
        <div style={{
            backgroundColor: 'aliceblue',
            borderRadius: '5px',
            color: 'black',
            padding: '1em',
            textAlign: 'center'
        }}>
            <h1>SCHEEEEEEE</h1>
            <p>Ca existe pas, lol</p>
        </div>
    </div >
);

export default NotFound;